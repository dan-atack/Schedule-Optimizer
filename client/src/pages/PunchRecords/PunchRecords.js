import React from 'react';
import styled from 'styled-components';

function PunchRecords() {
  return <Wrapper>Record of punches, KOs, and disqualifications</Wrapper>;
}

const Wrapper = styled.div`
  text-align: center;
`;

export default PunchRecords;
