import React from 'react';
import styled from 'styled-components';

function Notifications() {
  return <Wrapper>Notifications page here.</Wrapper>;
}

const Wrapper = styled.div`
  text-align: center;
`;

export default Notifications;
